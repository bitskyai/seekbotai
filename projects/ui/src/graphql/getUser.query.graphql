query GetUser {
  user {
    id
    preference {
      id
      apiKey
      logLevel
      logSize
      indexFrequency
    }
    ignoreURLs {
      id
      pattern
      regularExpression
    }
  }
}
