mutation createOrUpdatePages(
  $pages: [PageCreateOrUpdatePayload!]!
  $operationName: String = "createOrUpdatePages"
) {
  createOrUpdatePages(pages: $pages, operationName: $operationName) {
    url
    status
    id
    code
    message
  }
}
